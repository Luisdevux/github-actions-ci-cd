name: 01 - Building Blocks
on: push

jobs:
  workflow_dispatch:
    echo-hello:
      runs-on: ubuntu-latest
      steps:
        - name: Say hello
          run: echo "Hello, World!"
    echo-goodbye:
      runs-on: ubuntu-latest
      steps: 
        - name: Failed step
          run: ../imprime.sh
          continue-on-error: true
        - name: Say goodbye
          run: echo "Goodbye!"
          